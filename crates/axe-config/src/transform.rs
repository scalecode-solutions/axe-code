//! Transform pipeline for meta-variable manipulation.
//!
//! Transforms allow post-processing of captured meta-variables before
//! substitution in fix templates: substring, replace, case conversion, etc.

/// Placeholder — will be implemented with the transform system.
pub struct Transform {
    _private: (),
}
