//! Fix configuration and application.
//!
//! Improvements over ast-grep:
//! - Fix targeting: fixes can target a different node than the matched node
//! - Constraints on fixes: conditional application via `when` field
//! - TAB-aware indentation in replacement text

/// Placeholder — will be implemented with the fixer system.
pub struct Fixer {
    _private: (),
}
